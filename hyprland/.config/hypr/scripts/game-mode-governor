#!/usr/bin/env bash
set -euo pipefail
g="${1:-}"
case "$g" in
  performance|powersave) ;;
  *) echo "usage: $(basename "$0") performance|powersave" >&2; exit 1 ;;
esac
for f in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do
  echo "$g" > "$f"
done
